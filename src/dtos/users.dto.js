//=================================
//             Users
//=================================

export const sendVerificationRequestDTO = (req) => {
	return {
		email: req.email,
	};
};
export const getSampleResponseDTO = (req) => {
	return {
		sample: sample,
	};
};
